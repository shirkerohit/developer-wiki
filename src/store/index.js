import Vue from 'vue'
import Vuex from 'vuex'

import links from '../data/links.json'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    links
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
